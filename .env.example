# SQLite (default for local demo)
DATABASE_URL="file:./dev.db"

# PostgreSQL (production) — update provider in prisma/schema.prisma to postgresql
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/atomberg_goals"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-secure-random-string"

# Set to false in production to enforce quarterly windows
DEMO_MODE="true"
